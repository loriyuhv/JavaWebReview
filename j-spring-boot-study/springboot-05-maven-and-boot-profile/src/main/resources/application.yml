spring:
  profiles:
    active: ${profile.active}

---
spring:
  config:
    activate:
      on-profile: dev
server:
  port: 8081
logging:
  level:
    root: info
---
spring:
  config:
    activate:
      on-profile: pro
server:
  port: 8082
logging:
  level:
    root: info
---
spring:
  config:
    activate:
      on-profile: test
server:
  port: 8083
logging:
  level:
    root: warn